import styled from "styled-components";
import SearchTable from "../components/SearchMainContent";

function Search() {
  return (
    <StyleDiv>
      <SearchTable />
    </StyleDiv>
  );
}

export default Search;

const StyleDiv = styled.div``;
